# Layout

The application shell: sidebar, top-bar and the `<router-outlet>`
equivalent that hosts feature pages. Mirrors the Angular standard's
`layout/` folder.

- `AppLayout.tsx` — Shell component (sidebar + topbar + main content).
- `Sidebar.tsx` — Left navigation rail.
- `Toolbar.tsx` — Top action bar.
