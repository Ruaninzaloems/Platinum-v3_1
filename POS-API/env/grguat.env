# POS-API EMS profile: GrgUat (George UAT)
# Tenant users (User_UserDetail) on the George UAT SQL box; ems_v3 catalogue on Azure.
# Selected at startup via EMS_PROFILE=grguat (env var / App Setting).
PORT=3003

# Platinum API (login + user details for the George site)
PLATINUM_API_URL=https://georgeplatinumuatapi.azurewebsites.net

# ems_v3 catalogue (roles/modules/user_roles) — Azure SQL
EMS_V3_SERVER=emsfunctions.database.windows.net
EMS_V3_NAME=ems_v3
EMS_V3_USER=PlatinumV3User
EMS_V3_PASSWORD=e21Lpi5xctIU

# Tenant DB (User_UserDetail) — George UAT
# Local-network path (192.168.250.140) added for local dev testing of the new
# global Users & Roles security endpoints — same EMS_GeorgeUAT database, just
# reachable directly on the LAN instead of through the UAT box's public IP.
EMS_TENANT_SERVER=192.168.250.140
EMS_TENANT_PORT=1433
EMS_TENANT_USER=EMSRuanVDW
EMS_TENANT_PASSWORD=mU0ls31vMOooIigv
EMS_TENANT_NAME=EMS_GeorgeUAT
EMS_TENANT_ENCRYPT=false
EMS_TENANT_TRUST_CERT=true
